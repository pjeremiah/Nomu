# Production build: do not ship source maps (harder to read original files in DevTools).
# Secrets must NEVER go here — only REACT_APP_* public vars belong in frontend env files.
GENERATE_SOURCEMAP=false
REACT_APP_API_URL=https://nomu-backend.onrender.com
REACT_APP_ENVIRONMENT=production
